<?php
// Silence is golden.
$args = allianz_footer_opts([
	'default'       => $default,
	'default_value' => $default_value,
	'default_on'    => $default_off,
	'custom'        => $custom_opts
]);

return $args;