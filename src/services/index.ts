// Services Index - Centralized export for all services

export { HeaderManager } from './header';
export { CounterAnimationManager } from './counters';

// Service Types
export type {
  HeaderState,
  HeaderClasses,
  CounterData,
  CounterConfig,
  CounterOptions,
  ObserverOptions
} from '../types';